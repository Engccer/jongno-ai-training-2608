> 🤖 **이 파일은 자동 생성됩니다. 직접 수정하지 마세요.**
> 정본은 `CLAUDE.md` 입니다. 내용을 바꾸려면 `CLAUDE.md` 를 수정한 뒤
> 프로젝트 루트에서 `python sync_agent_docs.py` 를 실행하세요.
> 이 파일을 직접 고치면 다음 동기화 때 경고와 함께 덮어쓰기 대상이 됩니다.

<!-- SYNC-BODY-START: 이 줄 아래 본문은 CLAUDE.md 와 100% 동일하게 자동 생성됨 -->
# CLAUDE.md

> 이 파일은 Claude Code(claude.ai/code)가 이 저장소에서 작업할 때 참고하는 가이드다.

## 개요

종로구시각장애인취업역량강화센터 「재직자 AI 활용 직무향상과정(교사)」 김헌용 담당 4회기(2026-08-26~09-05)의 훈련생 배포용 강의 웹페이지. 훈련생 4명 전원이 전맹 센스리더 사용자이므로 슬라이드덱이 아니라 **원고 열람 페이지**다.

- 저장소: https://github.com/Engccer/jongno-ai-training-2608
- 배포 URL: https://engccer.github.io/jongno-ai-training-2608/
- 콘텐츠 정본: `G:\내 드라이브\KHY\Lectures\2608~2609 종로시각장애인복지관 재직자 AI 활용 직무향상과정(교사)\`의 `사전 준비 안내문.md`와 `5. 강의 원고\*.md`. 이 저장소는 그 내용을 옮긴 것이다. <!-- sanitize: allow 원고 정본 위치. 이 저장소의 존재 이유라 일반화 불가 -->

## 구조

- `build.py`: 드라이브의 마크다운을 읽어 `index.html`을 생성한다. 원고를 고치면 `python build.py` 후 commit·push. `SECTIONS`의 세 번째 값이 `False`인 회기는 본문 대신 「원고를 준비하고 있습니다.」만 실린다. 회기 원고가 확정되면 `True`로 바꾼다.
- `template.html`: 페이지 골격·CSS·JS. `{{BUTTONS}}`·`{{ARTICLES}}`를 build.py가 채운다.
- `index.html`: 생성물. 직접 편집하지 않는다.
- `AGENTS.md`: `CLAUDE.md`에서 자동 생성한 형제 문서. **이 저장소에는 `sync_agent_docs.py`가 없으므로** 머리말 안내와 달리 `agent-docs-sync` 스킬로 재생성하거나, 머리말 4줄과 `SYNC-BODY-START` 표시줄 아래에 `CLAUDE.md` 본문을 그대로 붙여 만든다. **`CLAUDE.md`를 고치면 반드시 함께 갱신한다.** 추적되는 사본이 낡는 것이 추적하지 않는 것보다 나쁘다.
- `sfx/open.mp3`·`close.mp3`: 열기·닫기 효과음(ElevenLabs sound-generation, 0.6초). 두 파일의 음량을 맞춰 둔다(-25~26 LUFS). 한쪽만 크면 그쪽이 크게 들린다.

### build.py의 표시 단계 변환 세 가지

정본 마크다운은 손대지 않고 여기서만 바꾼다. `.md`를 그대로 읽을 때 깔끔해야 하고, 링크·버튼은 표시 단계의 관심사다.

- `linkify`: 본문에 그대로 적힌 URL을 새 탭 링크로. 원고에 `[텍스트](주소)`를 쓰지 않는다.
- `unfence_in_lists`: 목록 항목 안에 들여쓴 ``` 펜스를 8칸 들여쓰기로. `fenced_code`가 열 0의 펜스만 처리해서 그냥 두면 인라인 `<code>`로 흘러 지시문이 본문에 묻힌다.
- `add_copy_buttons`: 코드 블록마다 위에 복사 버튼. 버튼은 반드시 `<pre>` **바깥**에 둔다(안에 넣으면 버튼 글자가 복사본에 섞인다).

## 동작

첫 화면은 h1과 제목 버튼 5개뿐이다. 버튼을 누르면 목록이 숨고 해당 `<article>`이 열리며 포커스가 그 h2로 간다. 닫기 버튼(본문 위·아래)이나 Esc로 닫으면 눌렀던 버튼으로 포커스가 돌아온다. URL 해시(`#s1`~`#s5`)로 딥링크와 뒤로 가기를 지원한다.

## 접근성

글로벌 접근성 헌장(`~/.claude/ACCESSIBILITY.md`)을 따른다. landmark는 `<main>` 하나, ARIA 없음, 네이티브 `<button>`·heading 계층(h1 페이지 → h2 문서 제목 → h3 원고 `##`)만으로 탐색한다. 원고 heading은 build.py가 한 단계씩 내린다. <!-- sanitize: allow 준수 기준 문서 참조. 경로가 곧 기준의 이름 -->

이 페이지에서 실측으로 얻은 규칙 셋.

- **복사 대상의 접근 가능한 이름에 보충 텍스트를 넣지 않는다.** 스크린 리더 가상 커서는 DOM 텍스트가 아니라 접근 가능한 이름을 복사하므로, 「새 탭에서 열림」을 `sr-only` span으로 넣든 `aria-label`로 넣든 복사본에 딸려 나와 붙여넣을 주소와 지시문이 깨진다(센스리더 실측, 두 방식 모두 재현). 링크 이름은 URL만 두고 새 탭 안내는 머리말에 한 문장으로 한 번만 둔다.
- **복사 버튼 이름은 「복사」뿐이다.** 무엇을 복사하는지는 바로 뒤 블록이 말해 준다. 결과는 페이지에 하나뿐인 polite live region(`#say`)으로 알리고 포커스는 버튼에 남긴다.
- **복사는 `textContent`로 한다.** 태그가 섞이지 않으면서 블록 안의 `<a>`는 그대로라 링크가 살아 있다.
